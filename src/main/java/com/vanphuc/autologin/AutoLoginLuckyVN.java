package com.vanphuc.autologin;

import net.fabricmc.api.ModInitializer;

public class AutoLoginLuckyVN implements ModInitializer {
	@Override
	public void onInitialize() {
		// Không cần thêm logic gì ở đây, vì mọi thứ đều chạy trên client.
	}
}